# Task List

1. Add V3 layout and width config validation.
2. Build padded render cells for raw, table, and transpose modes.
3. Cache the previous frame and draw only changed cells.
4. Replace foreground highlighting with full-cell background styles.
5. Add numeric alignment, truncation, and `No data` output.
6. Add resize pause, recovery, and forced redraw behavior.
7. Add V3 sample config and documentation.
8. Run V1, V2, and V3 compatibility and terminal lifecycle tests.
